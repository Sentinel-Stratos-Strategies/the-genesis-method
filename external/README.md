# External integrations

After cloning this repo, optionally link your **memory-contract** checkout here so gates and scripts resolve `external/memory-contract`:

```bash
ln -sf ../../projects/memory-contract external/memory-contract
```

Adjust the target path if your workspace layout differs.
